from ai_functions import summarize_document
import os 

# resume_folder = '/cv'
# resume_paths = [os.path.join(resume_folder, file) for file in os.listdir(resume_folder)]

# resume_summaries = [summarize_document(resume_path,summarize_resume_text) for resume_path in resume_paths]

# job_descirption_folder = '/jobs'
# job_descirption_paths = [os.path.join(job_descirption_folder, file) for file in os.listdir(job_descirption_folder)]

# summarize_document("/cv")